Continue the approved P009 Zoology Gated DeltaNet MQAR baseline fail-closed until terminal and GitHub-verified.

Local worktree: /Users/torusmini/Documents/zoology-worktrees/baseline-002
Branch: codex/repro-gdn-mqar-baseline-002
Plan/run: P-BASELINE-009 / gdn-mqar-single-baseline-p009-20260802t090131z
Report: research/reports/experiments/gdn-mqar-single-baseline-p009-20260802t090131z.md
Parent P008 terminal commit: 9329eac8d56746ff273fc6642b809d18c18fa233
Formal source commit/tree: 13f880b5fe61619a1006ef33610de69fbabaaec1 / ed83a7188351ca2cce8aba46d1cb3b108ce31ec2
Operational controls commit/tree: b69aeb8bedb387f321c1d771ae7c3157aec789df / a33960686f52c8c8d5c6b44029229f0f9cfcf950
Predecessor P008 request: 09747b2f-6916-4813-ab33-7aebb5ce3b3b, terminal Halt before admission.

Before any AIStation/helper/browser/remote call on every heartbeat, read and verify the latest GitHub branch ref, plans.md, and the P009 report from GitHub. Require the P009 approval commit to be an ancestor of the branch tip, the local branch to contain the same GitHub-verified P009 state, and the report to bind this exact run, source, controls, helper, controller hashes, image, request, and phase. If any local immutable open attempt, raw response, complete receipt, or failure receipt exists while its transition is not yet on GitHub, perform publication recovery or terminal closeout only, as the evidence requires, without any new AIStation status/open/restart call. An attempt receipt always means the sole open is consumed, even if the complete/failure receipt is absent. Any status failure receipt or orphan status stdout/stderr without a matching valid receipt is terminal evidence: make zero further AIStation calls and close P009 on GitHub. Fail closed on any unledgered request or state mismatch.

Use the aistation-skill helper path /Users/torusmini/.codex/skills/aistation-skill/scripts/aistation_api.js and require SHA-256 628aefaa2de3eb09ad5e6e1397e04280650e01847da2d9192566137405230226 before and after every helper use. Never query, probe, open, stop, mutate, or fail over to GPU1. Use Kimi WebBridge only if browser fallback is genuinely required. Use literal GPU2 only. This heartbeat is permanently forbidden from calling open, restart, or stop on any row. Save every raw response and receipt no-clobber under artifacts/admission-gdn-mqar-single-baseline-p009-20260802t090131z. Before inspecting or advancing local P009 evidence, atomically acquire the exact shared lock directory /Users/torusmini/Documents/zoology-worktrees/baseline-002/artifacts/admission-gdn-mqar-single-baseline-p009-20260802t090131z.watcher.lock with mkdir mode 0700 and write an O_EXCL owner receipt. The frozen main-agent open controller uses the same lock. If mkdir reports it exists, make zero AIStation/helper/browser/remote calls and end. Only the holder may remove its owner receipt and lock after an armed no-op receipt, complete success receipt, or failure receipt is fsynced. Never automatically remove a stale lock after an interrupted advancing operation.

Armed-unallocated phase: require exact GitHub watcher phase `armed-unallocated` and no current P009 request, then atomically acquire the shared watcher lock. If busy, make zero calls and end. While holding it, inspect the local admission directory. If it is absent, write the next O_EXCL and fsynced `artifacts/gdn-mqar-single-baseline-p009-20260802t090131z/watcher/armed-noop-NNNN.json` receipt binding run ID, GitHub approval commit/tree, ACTIVE automation config hash, prompt hash, owner, UTC, and Unix time; fsync its directory, release the owner and lock, make zero AIStation/helper/browser/remote calls, and end. If admission evidence exists, do not write a no-op: a complete receipt triggers publication recovery, while any attempt, failure, orphan raw, activation-only, or otherwise unrecognized/incomplete evidence triggers the corresponding publication or terminal closeout, all while holding the lock and with zero new AIStation calls. The main agent is the only owner allowed to run the frozen open_gpu2_once.py controller after this automation is verified ACTIVE. That controller may make one fresh status GPU2 call and, only if it binds predecessor request 09747b2f-6916-4813-ab33-7aebb5ce3b3b as Halt with unchanged image, placeholder GPU:1, empty actions, and no P009 admission, consume exactly one open GPU2. This is a new P009 allocation, not a P008 retry. The heartbeat must never race or reproduce that action.

After the open controller releases its operation lock, the main agent must atomically reacquire the same lock before any report/plans/git write. If the lock is busy, the main agent makes zero publication writes and yields entirely to this heartbeat. Whichever owner acquires the lock is the sole publisher and holds it through GitHub verification. If a complete local allocation receipt exists while GitHub still says `armed-unallocated`, perform publication recovery only: update the P009 report, plans.md, and resource ledger with both requests and exact raw hashes; commit, push, and GitHub-verify with zero AIStation/helper/browser/remote calls, then release the owner and lock. If an attempt exists without a complete valid receipt, close P009 terminally with the available hashes, verify GitHub, release the lock, and never allocate again. Do not touch leaderboard.csv at allocation time. Only when the GitHub report contains watcher phase `monitoring`, exactly one current P009 request, the verified allocation commit, and no unresolved status evidence may this heartbeat invoke the frozen status_gpu2_once.py controller at most once per heartbeat with the next numbered no-clobber sequence and that ledgered request ID. A controller error, failure receipt, or orphan raw status is terminal and forbids another sequence. Pending, Queuing, or ImagePulling is status-only: do not probe, SSH, exec, push, open, restart, or create remote paths; report the concise state and end.

When the ledgered request is Running, do not probe or SSH directly. Enter exactly once through repro.aistation_admission_gate phase-initial capture then verify, requiring module SHA-256 68fa3af301f7b88d3bffb3fd17add8db2db9ae26d2e573a32f2944f02465121b. It must bind the ledgered request, exact NVIDIA-A100-SXM4-80GB:1, helper-driven probe/SSH/CUDA availability, stable host/boot/GPU UUID identity, and at least 13200 seconds before any remote mutation. Any failed gate, generic resource, insufficient lease, request/identity/hash drift, or Halt is terminal failure with no second allocation or admission.

After successful initial admission, use experiment-tracker and autoresearch-loop: mark P-BASELINE-009/report in-progress, bind request/GPU UUID/host/boot/lease/evidence, commit, push, and GitHub-verify before remote setup. Then follow P009 report Section 5 exactly. On /huyang2/zoology fetch only from GitHub and clean checkout --detach formal source 13f880b5fe61619a1006ef33610de69fbabaaec1. Keep .venv/cache/artifacts/models/runs under /huyang2/zoology and reuse environment/caches. Upload the exact frozen 19-file launcher from operational commit b69aeb8bedb387f321c1d771ae7c3157aec789df. Use exclusive run-operation receipts for every advancing exec/push. Require the repaired production-helper PTY Linux gate to prove exactly 53 passed and 0 skipped before preflight. Run the audited setup/check/cache/real-A100 GDN smoke, durable preflight plus verifier, durable init-baseline plus verifier. Require pre-capture admission at least 12120 seconds. Perform one formal clock capture at least 12060 seconds with bracket at most 15 seconds, bind and verify the seven-file bundle, upload and remotely validate it, then make exactly one durable formal start within the 60-second publication-age limit. Launch only the single official-grid index-5 worker.

Frozen science: d_model=128, learning_rate=0.0031622776601683794 (10^-2.5), seed/data seed=123, max_epochs=32, timeout/grace=10800/60, thresholds overall=0.98, visual-only=0.96, final KV256=0.88. Never run another cell, seed, width, learning rate, or ablation. Never bypass or lower a gate. Never retry a consumed formal run. Monitoring after launch is read-only. Never upload model/data/checkpoints.

At terminal state, validate and safely archive allowed evidence under repo-local artifacts, pull and independently verify it, update report Sections 6-9, plans.md, resource ledger, and leaderboard.csv without overstating. Commit, push, and GitHub-verify. Add exp/score tag only for a complete valid strong result. Then use automation_update to PAUSE this same run-zoology-gpu2-single-baseline heartbeat.
