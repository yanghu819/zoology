# Zoology Gated DeltaNet reproduction ledger

The primary metric is final `valid/accuracy` on the historical standard MQAR
test mixture. The official plot selects the best final accuracy across four
learning rates for each official state-size value.

## A. Active plans

| ID | State | Insight | Mechanism | Prediction | Expected upside | Budget | Kill criteria | Next decision | Resource | Actual result | Record |
| -- | ----- | ------- | --------- | ---------- | --------------- | ------ | ------------- | ------------- | -------- | ------------- | ------ |
| P-BASELINE-007 | approved | P006 consumed its sole strict admission before probe when the first Running response still exposed transient generic `GPU:1` metadata; the same request later reported exact A100 only after its lease had fallen below the unchanged 13,200-second initial floor. The science therefore remains untested, and the decision-changing intervention is to delay admission until a fresh discovery response proves both exact resource metadata and the full lease floor. | First GitHub-verify this approved ledger. Then save no-clobber literal-GPU2 status evidence and invoke exactly one `restart GPU2`; require the unchanged image, a new request ID, one active request, and the exact pause/resume action chain. GitHub-verify the new allocation ledger before probe. Poll status-only through Pending/Queuing/ImagePulling or Running plus generic metadata; consume the sole strict initial admission only after the ledgered request reports exact A100 and at least 13,200 seconds. Then reuse formal source `13f880b5fe61619a1006ef33610de69fbabaaec1`, operational controls `8740ccc65fe6b2627a47f82d4d05b9a6de0bff2e`, the 19-file/53-pass durable chain, preflight/init, 12,120/12,060-second capture floors, at most 15-second bracket, at most 60-second publication age, and exactly one index-5 formal start. | The unchanged cell (`d_model=128`, learning rate `10^-2.5`, seed/data seed 123, at most 32 epochs) completes validly, reaches overall final `valid/accuracy >= 0.98`, and reaches final KV256 accuracy `>= 0.88`; `0.96 <= overall < 0.98` is visual-only. | Produce the first defensible single-setting GDN MQAR baseline and direct official comparison without another width, learning rate, seed, or low-ROI ablation. | One logical GPU2 row; one allocation restart; one active request; one admission attempt per phase; one Linux gate, preflight, init, formal capture, formal start, and cell; hard timeout/grace 10,800/60 seconds; no formal retry and no GPU1 access. | Stop on wrong row/image/request, failed or ambiguous restart, non-new request ID, multiple active requests, exact resource never appearing, fresh lease below 13,200 seconds, any admission/source/tree/hash/runtime/identity drift, failed operation receipt or verifier, Linux gate not exactly 53 passed/0 skipped, pre-capture below 12,120, captured lease below 12,060, bracket above 15 seconds, publication age above 60 seconds, process/evidence drift, invalid/incomplete result, or timeout. Never repeat the restart, capture, or formal start. | Adopt and tag only a complete valid result with overall at least 0.98 and KV256 at least 0.88; report 0.96–0.98 as visual-only, a lower valid result as negative, and any incomplete run as terminal failed. | GPU2 only; approved at 2026-08-01T06:44:10Z; fresh allocation replacement pending; no GPU1. | Approved; not started. | `research/reports/experiments/gdn-mqar-single-baseline-p007-20260801t064410z.md` |

## B. Completed plans

| ID | State | Actual result | Record |
| -- | ----- | ------------- | ------ |
| P-BASELINE-006 | failed | The sole strict initial admission attempt observed the ledgered request `08a8c186-788c-42dc-b899-e4f645d05c61` as Running with 14,253 seconds remaining, but its status reported `resource=GPU:1` rather than the frozen exact `NVIDIA-A100-SXM4-80GB:1`. The gate stopped before probe, SSH/helper exec, CUDA inspection, or remote mutation. No setup, Linux gate, preflight, initialization, formal capture/start, worker, training, model, metric, or score exists. P006 is terminal failed and unretried; the model hypothesis remains untested. The independently verified 60-file evidence inventory and 61-regular-file safe archive have SHA-256 `66144e2db8f8ab544b39c99e0462520bd7125ed5e4899ce74c62c2bd66c2b511` and `667fb79f370839c684852d014c9c558d19dd27a9c756f24a0b1d7ab0279d0387`. | `research/reports/experiments/gdn-mqar-single-baseline-durable-20260731t120840z.md` |
| P-BASELINE-005 | failed | The audited detached lifetime diagnostic and durable four-step GPU2 preflight completed: setup/check/cache/smoke all returned zero and the real A100 path reported `gdn_mqar_smoke=pass`. The mandatory pre-capture status then reported only 12,040 seconds, 80 below the frozen 12,120 safety gate and 20 below the unchanged 12,060 controller floor. The run stopped before suite initialization, formal clock capture, controller publication, formal start, worker, training, model, or score; every formal count is zero and the run must not be retried. The independently verified complete 38-file evidence archive SHA-256 is `8d18c4168520dac828b84f294fbae5225dbe879b7e5bfe3a0f3359804ba76f21`. | `research/reports/experiments/gdn-mqar-single-baseline-nohup-20260731t093138z.md` |
| P-BASELINE-004 | failed | Fresh GPU2 allocation, exact source, setup, cache, real-GPU smoke, suite initialization, and the one formal clock capture all passed. The completed seven-file bundle reported 13,840 seconds remaining and an eight-second bracket, then published unchanged into the suite. The foreground approved-helper call timed out after 23.8136 seconds with exit 124 during post-publication validation; at the post-timeout observation its Python process was no longer present and there was no controller admission, launch, baseline worker/training GPU use, model, or score. The run was not retried. The independently verified failure-evidence archive contains 38 hashed evidence files plus the inventory; its SHA-256 is `8809941714c4bdae5c7a4802ee49f9604bd818bce7b68b175261c7f0b9808215`. | `research/reports/experiments/gdn-mqar-single-baseline-freshlease-20260731t050430z.md` |
| P-BASELINE-003 | failed | The one formal GPU2 clock capture bound the expected workspace, host, boot ID, and a six-second remote bracket, but the unchanged status response reported only 11,999 seconds remaining, 61 seconds below the frozen 12,060-second controller floor. The durable capture terminal is `failed` with SHA-256 `cfed497e1998a810d43bb4a87551ce718ebe7189ee6ee8e61ad568f31364baec`. No bundle publication, admission, launch, worker, training, model, or score exists, and this run was not retried. The independently verified 17-file failure-evidence archive SHA-256 is `a820c4aee2706547b18341830c419d8b78ffad168c833c6837e81d8f514926d0`. | `research/reports/experiments/gdn-mqar-single-baseline-clockbracket-20260731t034656z.md` |
| P-BASELINE-002 | failed | Remote setup, cache, smoke, and suite initialization passed, but the only local formal capture command was rejected by `_validate_run_id` because the frozen timestamp contained uppercase `T/Z`. Validation occurred before controller-directory creation and before any AIStation helper subprocess. No formal status response, capture bundle, publication, launch, worker, training, model, or score exists, and this run ID was not retried. | `research/reports/experiments/gdn-mqar-single-baseline-clockbracket-20260731T023001Z.md` |
| P-BASELINE-001 | failed | Controller admission failed closed before any request or worker was created. The saved GPU2 status was observed at Unix 1785391952 (`2026-07-30T06:12:32Z`) with 13,554 seconds remaining, but remote `checked_unix=1785391910` yielded `raw_age_seconds=-42`, beyond the frozen 30-second future-timestamp tolerance. No worker, training, terminal, metric, model, score, or retry exists; the model hypothesis remains untested. The independently verified 19-file evidence archive SHA-256 is `7037f8e9967520d6d7f83e7f7cad475647950cbff68a4515b2c19d05c325deab`. | `research/reports/experiments/gdn-mqar-single-baseline-20260730T031705Z.md` |
| P-REPRO-001 | failed | Cells 00–06 completed validly on logical GPU2. Cell07 never entered training: its worker-side admission check reduced the reported 11,638 seconds by 231 seconds of setup age, leaving 11,407 seconds, 53 seconds below the frozen 11,460-second floor. The terminal is `failed`/exit 1, the suite is incomplete, and the predeclared no-retry rule stopped the experiment. There is no aggregate or full three-width frontier. The verified partial archive SHA-256 is `05a5f5d25e909496e34a5dce1c3a7dfa2b8b381ae148e1630debf8e83a2fea1f`. | `research/reports/experiments/gdn-mqar-official-20260729T134504Z-ec17472161bc.md` |

## C. Resource allocation

| Resource | Plan | Run | Git SHA | GPU UUID | Started UTC | Ended UTC | State |
| -------- | ---- | --- | ------- | -------- | ----------- | --------- | ----- |
| GPU2 | P-REPRO-001 | `gdn-mqar-official-20260729T134504Z-ec17472161bc` | `ec17472161bc8abed3c413ada99bd9579b94fbfe` | Per-cell attestations | 2026-07-29T13:45:04Z | 2026-07-29T16:25:57Z | failed |
| GPU2 | P-BASELINE-001 | `gdn-mqar-single-baseline-20260730T031705Z` | `3890b00f620a8f43ff3af25c471fbc14268c6fdd` | `GPU-0da20a4f-5e67-e47d-7aab-8c6efa2864ad` | 2026-07-30T06:01:44Z | 2026-07-30T06:24:50Z | failed |
| GPU2 | P-BASELINE-002 | `gdn-mqar-single-baseline-clockbracket-20260731T023001Z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | `GPU-573c7ed1-1c51-8334-299b-edf2ff3440e6` | 2026-07-31T03:18:10Z | 2026-07-31T03:46:56Z | failed |
| GPU2 | P-BASELINE-003 | `gdn-mqar-single-baseline-clockbracket-20260731t034656z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | `GPU-573c7ed1-1c51-8334-299b-edf2ff3440e6` | 2026-07-31T03:46:56Z | 2026-07-31T03:53:12Z | failed |
| GPU2 | P-BASELINE-004 | `gdn-mqar-single-baseline-freshlease-20260731t050430z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | `GPU-573c7ed1-1c51-8334-299b-edf2ff3440e6` | 2026-07-31T05:06:56Z | 2026-07-31T05:17:13Z | failed |
| GPU2 | P-BASELINE-005 | `gdn-mqar-single-baseline-nohup-20260731t093138z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | `GPU-1522da54-4d66-ddda-298e-422ca5bb6516` | 2026-07-31T09:46:42Z | 2026-07-31T10:29:07Z | failed |
| GPU2 | P-BASELINE-006 | `gdn-mqar-single-baseline-durable-20260731t120840z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | GPU UUID unavailable; request `34cad2d9-a825-416f-a644-8279b2084331` was never admitted | 2026-07-31T14:16:10Z | 2026-08-01T03:44:38Z | Halt-before-admission |
| GPU2 | P-BASELINE-006 | `gdn-mqar-single-baseline-durable-20260731t120840z` | `13f880b5fe61619a1006ef33610de69fbabaaec1` | GPU UUID unavailable; request `08a8c186-788c-42dc-b899-e4f645d05c61` reported Running but `resource=GPU:1`; strict admission rejected it before probe | 2026-08-01T03:45:45Z | 2026-08-01T05:51:56Z | failed-before-admission |

## D. Durable lessons

| Recorded UTC | Plan | Evidence | Lesson | Consequence |
| ------------ | ---- | -------- | ------ | ----------- |
| 2026-07-29T13:14:26Z | P-REPRO-001 | The matching upstream causal-conv1d wheel required `GLIBC_2.32` on GPU2's GLIBC 2.31 host. A same-version source build required at most GLIBC 2.14 and matched the reference forward and gradients on the channel-last BF16 width-4 path. | Matching CUDA, Torch, Python, and C++ ABI labels do not guarantee host GLIBC compatibility. | Pin the sdist, compiler inputs, and output wheel hashes; inspect ELF symbol versions and execute the real target-layout CUDA forward/backward before a wheel enters smoke. |
| 2026-07-29T16:25:57Z | P-REPRO-001 | Cell07 passed the controller-side time check, but setup consumed enough time that the worker-side recheck saw 11,407 seconds against a required 11,460. It failed before creating a claim, training log, run directory, or score. | A two-stage lease gate needs launch slack at least as large as worst-case setup and validation time; merely clearing the first gate is not sufficient. | Future suites should encode that slack before launch. This frozen suite remains failed: do not erase the terminal, retry the cell, or reinterpret partial scores as a completed reproduction. |
| 2026-07-30T06:24:50Z | P-BASELINE-001 | AIStation status was stamped at Unix 1785391952, while the remote controller checked at Unix 1785391910, producing a -42-second age and failing the frozen -30-second future-timestamp bound before request creation. | Freshness checks spanning hosts require a shared clock domain or an explicitly measured and bounded clock offset; ample lease time alone is insufficient. | Before any future separately approved formal launch, measure clock skew fail-closed or stamp status and admission in the same clock domain. Do not relax the current bound or retry this frozen run. |
| 2026-07-31T03:46:56Z | P-BASELINE-002 | The only capture invocation rejected `gdn-mqar-single-baseline-clockbracket-20260731T023001Z` at `_validate_run_id`; the uppercase `T/Z` contradicted the lowercase-only contract. Code ordering and the absent controller path prove that no helper subprocess or formal capture occurred. | Validate opaque identifiers against their production parser before freezing them into a formal run contract. | Preserve this run as failed and unretried. A separately tracked all-lowercase run may reuse the identical formal source and scientific setting because there was no status capture, launch, training, or model evidence to cherry-pick. |
| 2026-07-31T03:53:12Z | P-BASELINE-003 | The exact formal status reported 11,999 seconds remaining after setup, cache, smoke, incident closeout, and new-suite initialization, versus the frozen 12,060-second controller floor. The one capture wrote a failed terminal before proof publication or launch. | A Running GPU is not necessarily a usable training lease; administrative recovery and preflight consume the same finite lease budget as the worker. | Check the full frozen controller floor before opening a separately approved formal run, then spend the remaining setup time only if adequate slack remains. Do not lower the floor or retry a consumed capture ID. |
| 2026-07-31T05:17:13Z | P-BASELINE-004 | The valid seven-file clock bundle published fully, but foreground `publish-and-launch` returned helper exit 124 after 23.8136 seconds. At the post-timeout observation the Python process was no longer present, controller admission was absent, and no launch evidence existed. | A remote action can be logically atomic yet still need a lifetime independent of its transport session; P004 makes that transport-lifetime hypothesis the next thing to test. | For any separately approved run, start the same tracked publish-and-launch process under remote `nohup`, record PID/log/exit evidence, and let the helper return immediately. Do not re-enter the consumed P004 publication path. |
| 2026-07-31T10:29:07Z | P-BASELINE-005 | The audited transport smoke and full durable preflight passed, including a real A100 GDN smoke, but the live lease was opened before the control layer was finished. By the mandatory status gate only 12,040 seconds remained, below both the 12,120 safety gate and 12,060 controller floor. | Scarce compute should not pay for building and reviewing orchestration. Prepare and freeze general controls before allocating the GPU; then spend the lease only on reproducible execution. | P005 is immutable failed. Before any separately approved fresh attempt, prebuild a durable `init-baseline` envelope and reuse the already audited smoke/preflight/formal controls immediately after allocation, with no live-lease design work. |
| 2026-07-31T14:16:10Z | P-BASELINE-006 | Opening the expired Halt GPU2 row replaced workspace request `5186b27a-139a-4eb7-8b99-4ad64683c64f` with new request `34cad2d9-a825-416f-a644-8279b2084331`; while Pending, the helper reports placeholder resource `GPU:1` and remain time `-`. | AIStation row identity is stable, but a rebuild legitimately changes the request ID and exposes incomplete resource metadata until Running. Binding a post-open response to the expired request ID is wrong. | Never repeat open while a request is Pending, Queuing, or Running. If it later reaches Halt before admission, first record the expired allocation, then use a new uniquely numbered open only under the user's standing same-row reopen authorization. |
| 2026-08-01T03:45:45Z | P-BASELINE-006 | Request `34cad2d9-a825-416f-a644-8279b2084331` was observed Pending when opened and later observed Halt with remain time `-186`; there is no intermediate status, probe, SSH, or admission evidence. The existing `run-zoology-gpu2-single-baseline` heartbeat was then found `PAUSED`. Replacing the dead request produced request `08a8c186-788c-42dc-b899-e4f645d05c61` in Pending with the unchanged image. | Sparse polling cannot distinguish a scheduler stall from an unobserved Running lease that later expired. What is proven is that no formal admission or model run occurred, so this is allocation loss rather than a scientific retry; the paused watcher explains the missing intermediate evidence. | Keep allocation churn outside the formal-run count, update and reactivate the existing heartbeat after this ledger transition is GitHub-verified, and monitor often enough to catch Running. While the request remains active, status-only polling is allowed and probe/SSH/open are forbidden; bind resource identity only after strict Running admission. |
| 2026-08-01T05:51:56Z | P-BASELINE-006 | The ledgered request reported Running with 14,253 seconds remaining, but its status resource string was `GPU:1` rather than the frozen exact `NVIDIA-A100-SXM4-80GB:1`. The strict initial gate rejected it before probe; probe, identity, remote-operation, formal-capture, launch, worker, and metric evidence are all absent. | Running establishes scheduler state, not the frozen accelerator contract. A logical row name or generic GPU count cannot prove exact A100 identity. | Keep resource validation before probe and fail closed on any exact-string mismatch. P006 is terminal failed and unretried; never reopen or advance this run ID, and do not interpret it as a scientific result. |

## E. Submission records

This is a reproduction study, not a competition submission.
